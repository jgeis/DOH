# .env.example
# Copy this file to .env and fill in your actual database credentials
# DO NOT commit the .env file to version control!

# Database Selection
# Set to 'true' to use MSSQL (production), 'false' or leave unset for SQLite (local)
USE_MSSQL=false

# SQLite Configuration (local development)
SQLITE_DB_PATH=discharges.db

# MSSQL Server Configuration (production only)
DB_SERVER=your-server.database.windows.net
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
DB_DRIVER={ODBC Driver 17 for SQL Server}

# For Azure SQL:
# DB_SERVER=your-server.database.windows.net
# For local SQL Server:
# DB_SERVER=localhost
# or
# DB_SERVER=localhost\SQLEXPRESS

# Common ODBC Drivers:
# - {ODBC Driver 17 for SQL Server}
# - {ODBC Driver 18 for SQL Server}
# - {SQL Server Native Client 11.0}
